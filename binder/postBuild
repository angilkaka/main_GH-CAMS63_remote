#!/bin/bash

# Install the dependencies
#conda install -c conda-forge -y python=3.7 geoviews metpy xrviz xarray --file ./requirements.txt
echo "HALLLOOOO: ich bin hier: `pwd` "
conda install -c conda-forge -y python=3.7 geoviews metpy xrviz xarray --file ./requirements.txt

# Get rid of source code
#rm -rf xrviz
#rm -rf docs

